# ============================================================================
#  Local infrastructure environment.  Copy to `.env` (gitignored).
#  These are DEV-ONLY defaults. Never use these values outside local.
# ============================================================================

# --- PostgreSQL ---
POSTGRES_USER=ecommerce
POSTGRES_PASSWORD=ecommerce

# --- Grafana admin ---
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin
